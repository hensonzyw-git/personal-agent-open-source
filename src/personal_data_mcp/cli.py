"""Console entrypoint for personal-data-mcp."""

from __future__ import annotations


def main() -> None:
    """Refuse to start: DEV-001 only delivers the package skeleton."""
    raise SystemExit(
        "personal-data-mcp is a DEV-001 package skeleton with no runnable service. "
        "DEV-015 implements it; until then there is nothing to start."
    )
