# 公开快照验证

本快照分享代码与架构，不提供可直接部署的环境。历史私人环境验收不等于本快照验证。

## 离线检查入口

需要 Python 3.12、uv、Git；Swift 包需要兼容 Swift 6 的 macOS 工具链。
先安装锁定依赖：`uv sync --locked --extra adk`。测试前应使用独立 HOME、清空个人凭据环境，
在操作系统层限制私人文件读取及非本地网络访问。虚拟环境本身不提供这一隔离。

- Python：`.venv/bin/python -m pytest tests/`
- DAL 合同：`.venv/bin/python -m pytest tests/dal/test_registry_manifest_resolution.py tests/dal/test_wave3_artifact_receipts.py`
- Swift：`swift test --package-path ios/PersonalAgentKit`
- 打包：`uv build --no-sources`，然后在独立环境验证 wheel 的资源和导入。

不要运行 deploy 脚本或 live/provider 探针来验证本地导出。这些入口需要额外授权和自备环境。

## 早期快照准备结果（2026-09-23，历史改写前）

**当前为未定版候选。** Finance 的 20 条真实表达已从公开历史移除，公开数据改为纯合成；全历史扫描、构建与最终回执验证仍在进行，不能据此宣称可发布。

| 检查 | 观察结果 |
| --- | --- |
| 私人文件／外网隔离探针 | 私人源码内容读取与外网连接被拒绝；测试 HOME 可写 |
| 合同资源 | 源码与打包副本各 49 个 JSON，逐字节一致 |
| 合同重建回执 | 原生成器曾在早期单根候选上重建 14 份；新历史版必须重新绑定 |
| wheel／sdist | 构建成功；wheel 含 MIT 和 49 份合同；独立安装可加载 276 条迁移定义 |
| Python 完整套件尝试 | 状态机批量 oracle 回放在约 15 分钟后仍未完成，已终止；不能宣称全量通过 |
| 后续分批检查 | 4707 通过、51 失败、5 跳过；另有 3 个批量 oracle 用例未执行 |
| 临时目录路径复核 | 改用独立无空格临时目录后，相关 33 项回归通过，覆盖原 18 个失败项 |
| 迁移预期校准 | 仅修正 3 条旧 schema head 断言，相关 9 项回归通过，未改变迁移代码 |
| Swift | 编译成功；17 项 XCTest 通过；串行执行有 452 条 Swift Testing 通过记录，随后语音回调隔离测试进程以 signal 5 退出 |
| CI | 工作流已配置，尚未在 GitHub 执行，无通过徽章 |

上述分批结果不能相加后称为全量通过。当前仍有 30 个 Python 失败节点涉及
Worker／验证执行的原生沙箱路径；外层 macOS 隔离环境拒绝嵌套 sandbox-exec，
最小探针也复现此拒绝。这些测试未被删除或放宽，需要兼容的隔离主机继续验证。
另有 tokenizer 依赖用例因未提供运营环境的 tokenizer 文件而跳过。

Swift 语音源码和测试与固定源版本相同。本轮没有据此修复语音行为，也不把进程异常
归为已解决的环境问题。完整 oracle 回放和 Swift 回调验证均保持未完成状态。

这轮没有调用真实模型、访问个人业务数据服务、部署服务器或执行手机真机验收。

## 保留历史的公开候选验证（2026-09-23）

在固定私人基线的 934 个提交和 44 个合并节点上完成脱敏改写，逐个核对了父节点、作者与提交者时间、身份；三类差异计数均为零。Git 日期属于提交元数据，不是独立时间戳证明。

公开 Finance 数据集有 73 条案例：72 条合成、1 条 PRD 示例。其中 20 条为新写的 `PUB-*` 案例。公开版 reviewed-case 登记表为空；未经登记的真实样本标签仍被拒绝，测试专用合成见证覆盖了篡改输入、期望输出与标签降级。合同、模型评分、API 等定向测试通过。

新历史下 14 份机器合同重建回执由原生成器生成，独立校验及 17 类变异拒绝均通过。wheel 与源码包离线构建成功；独立安装的 wheel 加载并校验 73 条公开案例。历史全部对象和打包产物中，对已知 20 条真实表达及其私有摘要见证的精确匹配均为零。这个检查只覆盖已知样本与规则，不等同于任意个人信息均已被机器识别。

Gitleaks v8.30.1 的历史 diff 扫描有 2,575 条规则命中：2,544 条位于冻结合同中的 `idempotency_key` 测试值，25 条位于测试／夹具，5 条位于运行源码或打包测试向量，1 条位于合同生成器。另一个 `private-key` 命中是测试在运行时构造 PEM 的代码；设备认证向量明确标注其私钥只用于跨语言测试。未发现已确认的生产凭据，但规则扫描不替代人工隐私检查。

较早候选上的完整 Python 套件停滞、30 个沙箱相关失败节点，以及 Swift 语音回调崩溃尚未在本次公开候选上关闭。CI 尚未在 GitHub 运行。本候选是源码与架构的参考发布，不应描述为完整部署或真机验收通过。
